import Vue from 'vue';
//import Router from 'vue-router';

Vue.use(Router);

export default new Router({
    routes: []
    /*[
        { path: '/term/:language', name: 'terms', component: require('./pages/term.vue')},
        { path: '/term/:language/:term_id', component: require('./pages/hi.vue'),
            children: [
                { path: '', redirect: { name: 'term.suggestion' } },
                { path: 'suggestion', name: 'term.suggestion', component: require('./pages/suggestion-list.vue') },
                { path: 'comment', name: 'term.comment', component: require('./pages/comment-list.vue') },
            ] }
    ],
    mode: 'history'*/
});




